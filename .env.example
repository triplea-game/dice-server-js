# Database password - must match between the app and postgres containers
DB_PASSWORD=change-me

# SMTP credentials for sending dice roll and registration emails
SMTP_USER=your.email@provider.com
SMTP_PASS=your-smtp-password

